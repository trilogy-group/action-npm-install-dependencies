{
  "name": "cache",
  "version": "1.0.0",
  "private": true,
  "description": "Cache dependencies and build outputs",
  "scripts": {
    "build": "tsc && ncc build -o dist/ src/index.ts",
    "install-rdfind": "if command -v apt-get >/dev/null; then sudo DEBIAN_FRONTEND=noninteractive apt-get install -qq -o=Dpkg::Use-Pty=0 rdfind; elif command -v yum >/dev/null; then sudo amazon-linux-extras install epel -y &&  sudo yum install rdfind -y; else echo Cannot find an installer.; fi"
  },
  "devDependencies": {
    "@actions/cache": "^1.0.6",
    "@actions/core": "^1.2.6",
    "@actions/exec": "^1.0.4",
    "@actions/glob": "^0.1.1",
    "@actions/http-client": "^1.0.10",
    "@octokit/auth-action": "^1.3.2",
    "@octokit/rest": "^18.5.0",
    "@tsconfig/node12": "^1.0.7",
    "@types/node": "^12.0.4",
    "@vercel/ncc": "^0.27.0",
    "typescript": "^4.1.5"
  }
}
